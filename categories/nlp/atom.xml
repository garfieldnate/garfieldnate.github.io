<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: NLP | Garfield Nate]]></title>
  <link href="http://garfieldnate.github.io/categories/nlp/atom.xml" rel="self"/>
  <link href="http://garfieldnate.github.io/"/>
  <updated>2017-02-12T15:39:06+01:00</updated>
  <id>http://garfieldnate.github.io/</id>
  <author>
    <name><![CDATA[Nate Glenn]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[List of Japanese NLP Tools]]></title>
    <link href="http://garfieldnate.github.io/blog/2011/11/06/list-of-japanese-nlp-tools/"/>
    <updated>2011-11-06T00:00:00+01:00</updated>
    <id>http://garfieldnate.github.io/blog/2011/11/06/list-of-japanese-nlp-tools</id>
    <content type="html"><![CDATA[<p>I haven&rsquo;t tried out all of these so I don&rsquo;t have comments for everything, but hopefully this list will come in useful for someone.</p>

<h2>Morphological analyzers/tokenizers</h2>

<ul>
<li><a href="http://sourceforge.net/projects/itadaki/">Itadaki</a>: a Japanese processing module for OpenOffice. I&rsquo;ve done a tiny bit of work and issue documentation on a fork <a href="https://github.com/garfieldnate/itadaki">here</a>, and someone forked that to work with a Japanese/German dictionary <a href="https://github.com/wichmann/Itadaki">here</a>.</li>
<li>GoSen: Uses <a href="http://java.net/projects/sen/">sen</a> as a base, and is part of Itadaki; a pure Java version of ChaSen. See my previous post on where to download it from.</li>
<li><a href="http://mecab.sourceforge.net/">MeCab</a>: This page also contains a comparison of MeCab, ChaSen, JUMAN, and Kakasi.</li>
<li><a href="http://chasen.naist.jp/hiki/ChaSen/?%C3%E3%E4%A5%A4%CE%C7%DB%C9%DB">ChaSen</a></li>
<li><a href="http://nlp.ist.i.kyoto-u.ac.jp/EN/index.php?JUMAN">JUMAN</a></li>
<li><a href="http://code.google.com/p/cabocha/">Cabocha</a>: Uses support vector machines for morphological and dependency structure analysis.</li>
<li><a href="https://github.com/sile/gomoku">Gomoku</a></li>
<li><a href="http://igo.sourceforge.jp/">Igo</a></li>
<li><a href="https://github.com/atilika/kuromoji">Kuromoji</a>: Donated to Apache and used in Solr. Looks nice.</li>
</ul>


<h2>Corpora</h2>

<ul>
<li><a href="http://www.env.kitakyu-u.ac.jp/corpus/docs/index.html">Hypermedia Corpus</a></li>
<li><a href="http://www.sfs.uni-tuebingen.de/en/tuebajs.shtml">TüBa-J/S</a>: Japanese treebank from universityu of Tübingen. Not as heavily annotated as I&rsquo;d hoped. You have to send them an agreement to download it, but it&rsquo;s free.</li>
<li><a href="http://www.gsk.or.jp/catalog_e.html">GSK</a>: Not free, but very cheap.</li>
<li><a href="http://www.ldc.upenn.edu/">LDC</a>: Expensive unless your institution is a member</li>
</ul>


<h2>Other lexical resources</h2>

<ul>
<li><a href="http://kakasi.namazu.org/">Kakasi</a>: Gives readings for kanji compounds.</li>
<li><a href="http://nlpwww.nict.go.jp/wn-ja/index.en.html">WordNet</a>: Stil under development by NiCT. The sense numbers are cross-indexed with those in the English WordNet, so it could be useful for translation. Also, there are no verb frames like there are in English.</li>
<li><a href="http://cl.it.okayama-u.ac.jp/rsc/data/index.html">LCS Database</a>: From Okayama University</li>
<li><a href="http://jfn.st.hc.keio.ac.jp/index.html">Framenet</a>: Unfortunately you can only do online browsing.</li>
<li><a href="http://tell.fll.purdue.edu/chakoshi/public.html">Chakoshi</a>: Online collocation search engine.</li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Itadaki GoSen and IPADIC 2.7]]></title>
    <link href="http://garfieldnate.github.io/blog/2011/11/06/itadaki-gosen-and-ipadic-27/"/>
    <updated>2011-11-06T00:00:00+01:00</updated>
    <id>http://garfieldnate.github.io/blog/2011/11/06/itadaki-gosen-and-ipadic-27</id>
    <content type="html"><![CDATA[<p><strong>Update3: I&rsquo;ve forked the Itadaki project on <a href="https://github.com/garfieldnate/Itadaki">GitHub</a> to keep track of it better.</strong></p>

<p><strong>Update2: I made an executable JAR for GoSen that runs the ReadingProcessorDemo. It requires Java 6; just unzip the contents of <a href="https://sites.google.com/site/complingfiles/files/gosen.7z?attredirects=0&amp;d=1">this zip file</a> to your computer and click on the jar file.</strong></p>

<p><strong>Update1: The IPADIC dictionary is no longer available from its original location. It has been replaced by the <a href="http://sourceforge.jp/projects/naist-jdic/">NAIST</a> dictionary. I have edited the following post to reflect the needed changes.</strong></p>

<p>Itadaki is a software suite for processing Japanese in OpenOffice. GoSen, part of the Itadaki project, is a pure Java morphological analysis tool for Japanese, and I have found it extremely useful in my research. Unfortunately, the page for this project went down recently, making the tools harder to find. Itadaki is still available through Google code <a href="http://code.google.com/p/itadaki/">here</a>, but I can&rsquo;t find a separate installment of GoSen. The old GoSen website can still be accessed through the way-back-machine<a href="http://web.archive.org/web/20080108030419/http://itadaki.org/wiki/index.php/Itadaki"> here</a>. The other problem is that GoSen hasn&rsquo;t been updated since 2007, and in it&rsquo;s current release cannot handle the latest release of IPADIC. I&rsquo;ll describe how to fix it in this post.</p>

<p>Why does it matter that we can&rsquo;t use the latest version of IPADIC? Well, here&rsquo;s an example. I am using GoSen in my thesis work right now, and I put in a sentence which included a negative, past tense verb, such as 行かなかった. It analyzed it as な being used for negation, and かった being the past tense of the verb かう. That is indeed a problem! Using the newer IPADIC fixed it for me, though. To do that, download <a href="http://neu101.up.seesaa.net/etc/gosen-t001.zip">this</a> modified version of GoSen. The explanation for the fix is <a href="http://neu101.seesaa.net/article/182625342.html">here</a>. Basically, a change in the new IPADIC versions to work better with MeCab adds a bunch of commas that break GoSen.</p>

<p><strong>Edit: Once you&rsquo;ve downloaded and unzipped GoSen, run ant in the top directory to build an executable JAR file. Note that if you want javadoc, you&rsquo;ll have to change build.xml so that the javadoc command has &lsquo;encoding=&ldquo;utf-8&rdquo;&rsquo;. Next, you must download the IPADIC dictionary from its legacy repository, <a href="http://sourceforge.jp/projects/ipadic/downloads/24435/ipadic-2.7.0.tar.gz/">here</a>. Unpack the contents into testdata/dictionary. Change testdata/dictionary/build.xml so that the value of &ldquo;ipadic.version&rdquo; is &ldquo;2.7.0&rdquo; (the version that you downloaded). Now run ant in this directory to build the dictionary. [If you had errors, you may have forgotten to run ant in the top level directory first.]</strong></p>

<p>Then, to run a demo and see what amazing things GoSen can do, copy the dictionary.xml file from the testdata/dictionary directory to the dictionary/dictionary directory, go back to the root directory of GoSen, and then run <code>java -cp bin examples.ReadingProcessorDemo testData/dictionary/dictionary.xml</code>. The GoSen site says to run using the testdata folder, but that means you&rsquo;ll have to download the dictionary twice, which is dumb. When you run the above command, you&rsquo;ll get this GUI:</p>

<p><img class="center" src="/images/content_images/gosen.gif" width="209" height="320" title="&ldquo;Screenshot of GoSen&rsquo;s reading processor demo&rdquo;" ></p>

<p>Notice that it tokenizes the sentence, gives readings, and allows you to choose among alternatives analyses. It also gives information on part of speech and inflection.</p>

<p>To use GoSen in an Eclipse project, add gosen-1.0beta.jar to the project build path. You also need to have the dictionary directory somewhere, along with the dictionary.xml file. This code will get you started:</p>

<pre><code class="java Tokenizing with GoSen">package edu.byu.xnlsoar.jp.lexacc;
import java.io.IOException;
import java.util.List;

import edu.byu.xnlsoar.utils.Constants;

import net.java.sen.SenFactory;
import net.java.sen.StringTagger;
import net.java.sen.dictionary.Morpheme;
import net.java.sen.dictionary.Token;

public class GoSenInterface {
 public List&lt;token&gt; tokenize(String sentence){
  StringTagger tagger = SenFactory.getStringTagger(Constants.getProperty("GOSEN_DICT_CONFIG"));
  try {
   return tagger.analyze(sentence);
  } catch (IOException e) {
   e.printStackTrace();
   System.exit(-1);
  }
  return null;
 }

 public static void main(String[] args){
  String sentence = "やっぱり日本語情報処理って簡単に出来ちゃうんだもんな。";
  GoSenInterface dict = new GoSenInterface();
  System.out.println("tokenizing " + sentence);
  List&lt;token&gt; tokens = dict.tokenize(sentence);
  System.out.println(tokens);
  Morpheme m;
  System.out.println("surface, lemma, POS, conjugation");
  for(Token t : tokens){
   System.out.print(t + ", ");
   m = t.getMorpheme();
   System.out.print(m.getBasicForm() + ", ");
   System.out.print(m.getPartOfSpeech() + ", ");
   System.out.println(m.getConjugationalType());
  }
 }
}
</code></pre>

<p>If you run that you will get:</p>

<pre><code>tokenizing やっぱり日本語情報処理って簡単に出来ちゃうんだもんな。
[やっぱり, 日本語, 情報処理, って, 簡単, に, 出来, ちゃう, ん, だ, もん, な, 。]
surface, lemma, POS, conjugation
やっぱり, やっぱり, 副詞-一般, *
日本語, 日本語, 名詞-一般, *
情報処理, 情報処理, 名詞-一般, *
って, って, 助詞-格助詞-連語, *
簡単, 簡単, 名詞-形容動詞語幹, *
に, に, 助詞-副詞化, *
出来, 出来る, 動詞-自立, 一段
ちゃう, ちゃう, 動詞-非自立, 五段・ワ行促音便
ん, ん, 名詞-非自立-一般, *
だ, だ, 助動詞, 特殊・ダ
もん, もん, 名詞-非自立-一般, *
な, だ, 助動詞, 特殊・ダ
。, 。, 記号-句点, *
</code></pre>

<p>You have plenty of other options while processing, like grabbing alternate readings, etc. Notice that it got one wrong here: ちゃう is a contraction of てしまう, not a verb whose lemma is ちゃう. It doesn&rsquo;t seem to work on contractions because every token needs a surface form. So this might not work well on informal registers such as tweets or blogs unless some pre-preprocessing is done.</p>

<p>Feel free to leave any questions or comments.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[String Allignment With Edit Operations]]></title>
    <link href="http://garfieldnate.github.io/blog/2011/09/01/string-allignment-with-edit-operations/"/>
    <updated>2011-09-01T00:00:00+02:00</updated>
    <id>http://garfieldnate.github.io/blog/2011/09/01/string-allignment-with-edit-operations</id>
    <content type="html"><![CDATA[<p>A common way to measure the distance between two strings is using Levenshtein distance. Levenshtein distance is the minimum number of deletions, insertions, and substitutions needed to transform one string into another. Finding the distance between two strings is useful in certain applications such as spell checking (a word processor will suggest dictionary words that are close to your misspelled word). See <a href="http://en.wikipedia.org/wiki/Levenshtein_distance">wikipedia</a> for more details and an example of Levenshtein distance calculation.</p>

<p>Another related and also important operation is to find the minimum edit alignment; that is, once the minimum edit distance between the two strings is found, output the sequence of operations that can be used to change the one string into the other. For example, if we let C mean &ldquo;correct&rdquo;, S mean &ldquo;substitution&rdquo;, D mean &ldquo;deletion&rdquo; and I mean &ldquo;insertion&rdquo;, then the edit alignment between the characters in &ldquo;construction&rdquo; and &ldquo;distortions&rdquo; would be ISSCCISSCCCCD. Here is an explanation of the alignment:</p>

<ul>
<li>I: Insert a &ldquo;c&rdquo; ->cdistortions</li>
<li>S: Substitute &ldquo;d&rdquo; for &ldquo;o&rdquo; ->coistortions</li>
<li>S: Substitute &ldquo;i&rdquo; for &ldquo;n&rdquo; -> constortions</li>
<li>CC: Leave the &ldquo;st&rdquo; alone</li>
<li>I: Insert &ldquo;r&rdquo; -> constrortion</li>
<li>S: Substitute &ldquo;u&rdquo; for &ldquo;o&rdquo; -> contrurtion</li>
<li>S: Substitute &ldquo;c&rdquo; for &ldquo;r&rdquo; -> constructions</li>
<li>CCCC: Leave &ldquo;tion&rdquo; alone</li>
<li>D: delete &ldquo;s&rdquo; -> construction</li>
</ul>


<p> This sort of an allignment operation is commonly used in grading the output of machine translation and speech recognition systems, though the operations are done at the word level instead of the character level. When I needed a quick piece of code to do the alignment in my own application, though, there was none to be found on the internet! So I made my own and am posting it here. The logic for determining the proper string of edit operations is taken from a StackOverflow answer <a href="http://stackoverflow.com/questions/5849139/levenshtein-distance-inferring-the-edit-operations-from-the-matrix/5861206#5861206">here</a>. And now the code:</p>

<pre><code class="java align two strings via edit operations">/**
     * @return List of Operations representing allignment between list1 and
     * list2. The allignment represents operations to change list2 into list1.
     */

    public static List levenshteinAllignment(Object[] list1, Object[] list2) {
        int[][] distanceMatrix = getDistanceMatrix(list1, list2);
        List ops= new ArrayList(
                list1.length &gt; list2.length ? list1.length : list2.length);
        //think of distance chart as going from bottom left to top right;
        //current position coordinates; start at top right.
        int row = list1.length;
        int col = list2.length;
        //could have moved to current position from three others; store their scores here.
        int diag;
        int left;
        int below;
        int current;

        while (row != 0 || col != 0) {
            diag = getVal(row-1,col-1,distanceMatrix);
            left = getVal(row,col-1,distanceMatrix);
            below = getVal(row-1,col,distanceMatrix);
            current = distanceMatrix[row][col];

//             if the value in the diagonal cell (going up+left) is smaller or equal to the
//                values found in the other two cells

//             AND
//                if this is same or 1 minus the value of the current cell
            if(diag &lt;= left &amp;&amp; diag &lt;= below &amp;&amp; (diag == current || diag == current - 1)){
//                 then  "take the diagonal cell"
//               if the value of the diagonal cell is one less than the current cell:
                if(diag == current - 1)
//                  Add a SUBSTITUTION operation (from the letters corresponding to
//                  the _current_ cell)
                    ops.add(new Operation(Operation.Type.SUBSTITUTION,list1[row-1],list2[col-1]));
                else
//                  otherwise: do not add an operation this was a no-operation.
                    ops.add(new Operation(Operation.Type.CORRECT,list1[row-1]));
                //move diagonally
                row--;
                col--;
            }
//
//          elseif the value in the cell to the left is smaller or equal to the value of
//              the cell below current cell
//          AND
//          if this value is same or 1 minus the value of the current cell
            else if(left &lt; below &amp;&amp; (left == current || left == current - 1)){
//              add an INSERTION of the cell to the left
                ops.add(new Operation(Operation.Type.INSERTION,list2[col-1]));
                //move left
                col--;
            }
//          else
            else{
//             take the cell below, add
//             Add a DELETION operation
                ops.add(new Operation(Operation.Type.DELETION,list1[row-1]));
                //move down
                row--;
            }
        }
        Collections.reverse(ops);
        return ops;
    }
    private static int getVal(int row, int col, int[][] distanceMatrix){
        if(row &lt; 0 || row &gt; distanceMatrix.length)
            return Integer.MAX_VALUE;
        if(col &lt; 0 || col &gt; distanceMatrix.length)
            return Integer.MAX_VALUE;
        else return distanceMatrix[row][col];
    }
    public static class Operation{
        private Type type;
        private Object object1;
        private Object object2;
        public enum Type{
            CORRECT,SUBSTITUTION,DELETION,INSERTION
        }
        public Operation(Type t, Object o1){
            type = t;
            object1 = o1;
            object2 = null;
        }
        public Operation(Type t, Object o1, Object o2){
            type = t;
            object1 = o1;
            object2 = o2;
        }
        @Override
        public String toString(){
            if(type == Type.SUBSTITUTION)
                return "Substitution: " + object1.toString() + " -&gt; " + object2.toString();
            if(type == Type.CORRECT)
                return "Correct:      " + object1.toString();
            if(type == Type.DELETION)
                return "Deletion:     " + object1.toString();
            if(type == Type.INSERTION)
                return "Insertion:    " + object1.toString();
            return null;
        }
    }

    /**
     *
     * @param array of objects to compare
     * @param array of objects to compare
     * @return Levenshtein distance between arrays.
     * This method uses the equals(Object o) method to compare the
     * objects in the two arrays, returning the Levenshtein distance between them.
     */
    public static int levenshteinDistance(Object[] list1, Object[] list2) {

        int[][] distance = getDistanceMatrix(list1, list2);
        return distance[list1.length][list2.length];
    }

    /**
     *
     * @param list1
     * @param list2
     * @return A completely filled distance matrix; movement from [i-1][j]
     *         represents insertion, from [i][j-1] represents deletion, and from
     *         [i-1][j-1] represents substitution or no operation.
     */
    private static int[][] getDistanceMatrix(Object[] list1, Object[] list2) {
        int[][] distanceMatrix = new int[list1.length + 1][list2.length + 1];

        for (int i = 0; i &lt;= list1.length; i++)
            distanceMatrix[i][0] = i;
        for (int j = 0; j &lt;= list2.length; j++)
            distanceMatrix[0][j] = j;

        for (int i = 1; i &lt;= list1.length; i++)
            for (int j = 1; j &lt;= list2.length; j++)
                distanceMatrix[i][j] = minimum(distanceMatrix[i - 1][j] + 1,// insertion
                        distanceMatrix[i][j - 1] + 1,// deletion
                        distanceMatrix[i - 1][j - 1]// substitution or correct
                                + ((list1[i - 1].equals(list2[j - 1])) ? 0 : 1));
        return distanceMatrix;
    }

    /**
     * Same as Math.min, but returns the minimum of three arguments instead of
     * two.
     */
    private static int minimum(int a, int b, int c) {
        return Math.min(Math.min(a, b), c);
    }
</code></pre>

<p>For word level alignment, you can call it on Strings using the split function like so:</p>

<pre><code class="java example string alignment">for(Operation o : levenshteinAllignment(
            "What My house gleams with the light of the moon and your face"
                    .split(" "),
            "Your house with the light of the the moon and my face"
                    .split(" "))
        )
            System.out.println(o);
</code></pre>

<p>And the output would be:</p>

<pre><code>Deletion:     What
Substitution: My -&gt; Your
Correct:      house
Deletion:     gleams
Correct:      with
Correct:      the
Correct:      light
Correct:      of
Correct:      the
Insertion:    the
Correct:      moon
Correct:      and
Substitution: your -&gt; my
Correct:      face
</code></pre>

<p>Feel free to use and edit this as you like. Many applications disregard the strings that are correct and only output the edit operations, and that should be an easy edit.</p>
]]></content>
  </entry>
  
</feed>
